# SweetDependency project configuration file
# You can adjust your custom configuration to your liking here
# You can visit https://github.com/HighCapable/SweetDependency for more help
#
# SweetDependency 项目配置文件
# 你可以在这里调整你喜欢的自定义配置
# 你可以前往 https://github.com/HighCapable/SweetDependency 以获得更多帮助

# Configure preferences
# 配置偏好设置
preferences:
  autowire-on-sync-mode: UPDATE_OPTIONAL_DEPENDENCIES
  repositories-mode: FAIL_ON_PROJECT_REPOS

# Configure repositories used by dependencies
# 配置依赖使用的存储库
repositories:
  gradle-plugin-portal:
    scope: PLUGINS
  google:
  maven-central:

# Configure plugins that need to be used
# For example:
# plugins:
#   org.jetbrains.kotlin.jvm:
#     version: +
#
# 配置需要使用的插件依赖
# 例如:
# plugins:
#   org.jetbrains.kotlin.jvm:
#     version: +
plugins:
  org.jetbrains.kotlin.android:
    version: 2.0.20

# Configure libraries that need to be used
# For example:
# libraries:
#   com.google.code.gson:
#     gson:
#       version: +
#
# 配置需要使用的库依赖
# 例如:
# libraries:
#   com.google.code.gson:
#     gson:
#       version: +
libraries:
  # libxposed API 签名中引用了 androidx 注解，需要提供
  androidx.annotation:
    annotation:
      version: 1.8.2
  # libxposed API 102（模块入口 / Hook API）
  # 作为 compileOnly 依赖，运行时由支持 libxposed 的框架提供
  io.github.libxposed:
    api:
      version: 102.0.0
    # libxposed Service（模块 app 侧写入框架远程偏好 Remote Preferences）
    service:
      version: 102.0.0
